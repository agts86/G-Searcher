{
  "name": "@api-ts/infrastructure",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "echo \"no tests in infrastructure (covered by features/*/test integration tests)\" && exit 0",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@api-ts/tables": "workspace:*",
    "@api-ts/shared": "workspace:*",
    "@api-ts/features-auth": "workspace:*",
    "@prisma/client": "^6.1.0"
  },
  "devDependencies": {
    "typescript": "^6.0.0",
    "@types/node": "^22.10.0"
  }
}
