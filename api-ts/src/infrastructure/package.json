{
  "name": "@api-ts/infrastructure",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage --coverage.reporter=text-summary",
    "lint": "eslint src test --ext .ts"
  },
  "dependencies": {
    "@api-ts/tables": "workspace:*",
    "@api-ts/shared": "workspace:*",
    "@api-ts/features-auth": "workspace:*",
    "@api-ts/features-managed": "workspace:*",
    "@api-ts/features-webhook": "workspace:*",
    "@prisma/client": "^6.1.0",
    "@line/bot-sdk": "^11.2.0"
  },
  "devDependencies": {
    "typescript": "^6.0.0",
    "@types/node": "^22.10.0",
    "vitest": "^3.0.0"
  }
}
