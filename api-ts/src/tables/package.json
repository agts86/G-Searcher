{
  "name": "@api-ts/tables",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "prisma generate && tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "echo \"no lint target in tables\" && exit 0"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0"
  },
  "devDependencies": {
    "prisma": "^6.1.0",
    "typescript": "^6.0.0",
    "vitest": "^3.0.0",
    "@types/node": "^22.10.0"
  }
}
