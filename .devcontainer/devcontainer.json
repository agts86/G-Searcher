{
	"name": "LineWebHookAPI",
	"dockerComposeFile": ["../docker-compose.yml", "docker-compose.devcontainer.yml"],
	"service": "server",
	"workspaceFolder": "/workspaces/LineWebHookAPI",
	"remoteUser": "root",
	"forwardPorts": [3000, 3001, 5432],
	"customizations": {
		"vscode": {
			"extensions": [
				"dbaeumer.vscode-eslint",
				"biomejs.biome",
				"Prisma.prisma",
				"ms-azuretools.vscode-docker"
			]
		}
	},
	"postCreateCommand": "cd /workspaces/LineWebHookAPI && pnpm install --store-dir /tmp/pnpm-store && cd server && pnpm install --store-dir /tmp/pnpm-store && pnpm --filter @api/tables exec prisma generate && cd ../web && pnpm install --store-dir /tmp/pnpm-store --ignore-workspace"
}
